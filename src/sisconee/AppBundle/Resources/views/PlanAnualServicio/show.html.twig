{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PlanAnualServicio</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Plan</th>
                <td>{{ entity.plan }}</td>
            </tr>
            <tr>
                <th>Anno</th>
                <td>{{ entity.anno }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('plananualservicio') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('plananualservicio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
