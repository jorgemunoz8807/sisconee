{% extends 'sisconeeAdministracionBundle:Default:layout_admin.html.twig' %}


{% block content %}


        <h1>Editar tipo de servicio: {{ entity.descripcion }} </h1>

        {% include "::flashmessages.html.twig" %}
        {% include "::modal-delete.html.twig" %}

        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <div class="form-actions">
            <input type="submit" class="btn btn-primary" value="Aceptar" />
            <a class="btn btn-default" href="{{ path('administracion_tiposervicio') }}">Regresar al listado</a>

            <a class="btn btn-danger link-eliminar" style="margin-left:50px;" href="javascript:void(0)"
               data-url="{{ path('administracion_tiposervicio_delete', { 'id': entity.id }) }}"
               data-descripcion="{{ entity.descripcion }}"
               data-toggle="modal" data-target="#modal-delete">
                <span class="glyphicon glyphicon-trash"></span>Eliminar
            </a>
        </div>
        {{ form_end(edit_form) }}



{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('layoutadmin/admin-list.js') }}"></script>
{% endblock %}



