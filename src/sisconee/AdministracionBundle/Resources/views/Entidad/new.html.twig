{% extends 'sisconeeAdministracionBundle:Default:layout_admin.html.twig' %}


{% block content %}


    <h1>Adicionar Entidad</h1>

    {% include "::flashmessages.html.twig" %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-actions">
            <input type="submit" class="btn btn-primary" value="Aceptar" />
            <a class="btn btn-default" href="{{ path('administracion_entidad') }}">Regresar al listado</a>
        </div>
        {{ form_end(form) }}

    {#<div class="form-group col-sm-6">
        {{ form_widget(form.idEntidad, {'attr': {'class': 'form-control'}}) }}
    </div>

    <div class="form-group col-sm-6">
        {{ form_widget(form.codreeup, {'attr': {'class': 'form-control'}}) }}
    </div>#}

    {#{{ form_row(form.codreeup, {'attr': {'class': 'form-control'}}) }}#}

   {# {% if user. == 'PS' %}
        <div>
            es PS
        </div>
    {% else %}
        <div>
            {{ rol }}
        </div>
    {% endif %}
#}

{% endblock %}
